---
interface Props {
  /** Zero-based index. Must match the `data-steps` values on the chart layers. */
  step: number;
}

const { step } = Astro.props;
---
<div class="scrolly-step prose" data-step={step}>
  <slot />
</div>
